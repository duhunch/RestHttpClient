using System;
using System.Collections;
using System.Collections.Generic;

//namespace Dragos.Net.Client.Parameters.XmlRpc
//{
    //public class ValueParamFactory
    //{
    //    public static IParamValue Create(object value)
    //    {
    //        if (value is string)
    //            return new StringParam(value);
    //        if (value is int)
    //            return new IntParam(value);
    //        if (value is DateTime)
    //            return new DateTimeParam(value);
    //        if(value is double)
    //            return new DoubleParam(value);

    //        if (value is IDictionary)
    //        {
    //            var param = new
    //        }
    //        if (value is IEnumerable)
    //        {
    //            var param = new List<IParamValue>();
    //            foreach (var item in value as IEnumerable)
    //            {
    //                param.Add(Create(item));
    //            }
    //            return new ArrayParam(param);
    //        }
    //        throw new ArgumentOutOfRangeException(nameof(value));
    //    }
//    }
//}
